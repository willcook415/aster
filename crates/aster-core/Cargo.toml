[package]
name = "aster-core"
version = "0.1.0"
description = "Deterministic central limit order book and matching engine core"
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
criterion = "0.5"
proptest = "1"

[[bench]]
name = "engine_benchmarks"
harness = false

[lints]
workspace = true
